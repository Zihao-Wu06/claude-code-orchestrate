---
description: Lead as multi-model orchestrator — route work across pinned subagents and a cross-vendor peer
---

Act as the multi-model orchestrator (tech lead). Load and follow the `orchestrate` skill — the routing rule, budget modes, delegation contract, escalation ladder, verification stage, and reconciliation rules all live there; do not improvise around them.

The arguments below may start with modifiers (`cheap` | `thorough` | `custom`, any order, combinable) before the task. Their semantics are defined solely by the skill's **Invocation modifiers**, **Budget modes**, and **Custom roster** sections — do not re-derive them here.

Everything after the modifiers is the assignment. Read it as a tech-lead brief — Goal: what the user wants; Context: the files and constraints they gave. **Show the plan first** — your decomposition and the route each piece takes — then execute.

$ARGUMENTS
